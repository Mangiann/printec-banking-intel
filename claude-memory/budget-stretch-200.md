---
name: budget-stretch-200
description: "The \"Stretch to EUR 200m\" view added to the Budget tab on 22/09/2026 — the EUR 180.0m budget taken to EUR 200.0m from new business and the payments industry only, no named deal changed; engine step `board_2027.py stretch`, five layers, its own card, third estimate card, stretch scenario in the moves grid and the category view, chat tool"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0bb1fab0-f44e-4aec-8f89-37f3eb288af0
  modified: 2026-09-22T11:48:56.053Z
---

**Ask (user, 22/09/2026):** "a new tab/selection showing the stretched budget, going to 200m; use anything you can to augment it; do not touch the named deals; focus on new business and the payments industry."

**Engine:** `weekly-intelligence/scripts/board_2027.py stretch --phase numbers|texts` → `budget/budget_2027_stretch.json` + `budget/board/stretch/STRETCH_BRIEF.md`; a fable writer wrote `budget/board/stretch/stretch_texts.json` (headline, summary_md with 5 bold leads, 8 conditions, 5 risks; banned-word checked) merged by the texts phase into `S["texts"]`. Payments lines = pos_acquiring, payments_instant, fraud_monitoring, digital_onboarding, payment_security_hsm, card_issuing. Five layers, all deterministic from the data, summing exactly to EUR 200.0m (target constant STRETCH_TARGET):
1. New business at the plan's own rate: the 60 country×product combinations with opportunities but no plan number (from budget_2027.json `new_business` minus the 29 priced), priced at the RATES THE PLAN ITSELF USED for its 29 (target / opportunity value, footprint-wide items at half weight): 5.3% with a 2027-dated opportunity, 3.6% without → EUR 10.18m.
2. Payments waves counted in every payments line they touch at full weight (footprint-wide share items "listed but not counted" get their points, counted ones the other half) → EUR 0.24m.
3. Acquiring estates at the full terminal-market rate (POS recurring_2026 × (market_pct − recurring_rate_pct)) → EUR 0.38m.
4. One year of the payments industry's growth from the Future outlook on the payments software lines (identity 15.4%, AML 18.0%, HSM 15.3%, instant payments 15.0%; `PAY_GROWTH`) → EUR 1.20m.
5. Payments combinations converting at a higher rate: the uplift that closes the gap to 200.0 (5.1 points → 8.8% of opportunity value), capped at the plan's best-third rate (20.3%); if capped, `unsupported` shows the remainder → EUR 8.01m.
Per cell: existing cells = budget + layers 2–4 (`cells[].extra`); new combinations in `new_cells` (60, with rate, largest signal, reason); `by_country`/`by_line` (sum 199.7 because the small-cell 0.3m sits only in totals); `new_business_lines` (key management 5.46m/12 countries, digital identity 2.96m, fraud 2.62m, payments modernisation 1.60m, physical security 1.30m, core/digital 1.17m, compliance 1.15m, card issuing 0.97m…); `capabilities` (the plan's capability decisions), `futures` (payments themes of the 2030 outlook), `rp_stretch` (55% group margin on the added revenue).

**Dashboard:** third estimate card "Stretch to €200m" (k-lilac; `SCEN.stretch`); the moves grid shows per line budget + payments extras, totals anchored on the stretch file's country/line totals, grand total €200.0m; drill-down row "Stretch to €200m" where a line has an extra; the category view has a `stretch` scenario (budget_categories.py: stretch pseudo-cells for the new combinations at the group mix of their line, source kind `stretch`; Excel gets a "Stretch 2027" column); the stretch card `.bgs` after the drivers card: equation line, horizontal waterfall, the writer's narrative, five layer tiles (items fold + top-signals fold without euros), "What the new business is" by line with countries, the capability decisions, the Future outlook themes, what must be true, the risks, the rates footnote. Chat: glossary (1c), fact-sheet line "STRETCH TO EUR 200.0m", tool `get_stretch(country?)`, `stretch_2027` field on `get_budget_line`, prefetch on "stretch|200m". Artifact v92, build 22/09/2026 14:47.

**Honesty points to keep:** the stretch rests on conversion of signals, not signed orders (only 11 of the 60 combinations carry a 2027-dated opportunity); the rates stay inside what the plan accepted; layer 5 is the judgement layer and is labelled as such. Related: [[budget-board-2027]], [[budget-by-category]], [[dashboard-ask-chat]].

**Naming (user, 22/09/2026):** the green card is "2027 base budget" (no tagline under the figure); the label follows into the moves-table caption, the category table header and the chart end labels (`SCEN.budget.label` set when `single_number`). The New business driver tile has a fold "What the new business is" listing the product lines by name only, no countries, no amounts. Artifact v93, build 22/09/2026 15:05.

**Two sidebar entries (user, 22/09/2026):** the sidebar group is "BUDGET 2027" with two nested entries: "Base budget" (tab id `budget`, renders `renderBudget(s,'base')`: hero with 2026 + 2027 base budget cards, drivers card, summary, full explanation, category view, moves grid) and "Stretched budget" (tab id `budget_stretch`, `renderBudget(s,'stretch')`: hero "The stretched budget" with the stretch narrative's first paragraph and two cards, 2027 base budget (reference) + Stretched budget €200.0m selected, then the stretch card, the category view and the moves grid on the stretch scenario; no drivers card). buildShell gates both on budget data; `RENDER` maps both ids; the unlock still opens `budget`. Chat SUGG has `budget_stretch` questions; glossary (1) describes the two entries. Artifact v94, build 22/09/2026 15:19.

**Wording (user, 23/09/2026):** layer one is titled "New business, priced the way the plan priced its own" and its text explains the yardstick (what the plan set for its 29 new-business combinations divided by the opportunity value behind them: 5.3% with a 2027-dated opportunity, 3.6% without); the per-combination reason and the chat glossary say the same; the writer's narrative keeps "5.3% and 3.6% are the plan's own rates" as the explanation. Rerunning `stretch --phase numbers` clears `texts`, so always follow with `--phase texts`. Artifact v95, build 23/09/2026 13:12. "Yardstick" was replaced by "the same rule the base budget used" (user asked what it meant, 23/09): artifact v97, build 13:20. Layer two renamed "Regulations and industry changes counted in every payments line they affect" with a plain explanation (a change that hits every bank at once, counted once at half weight in the base, in every payments line at full weight in the stretch; €0.24m, nine cells): artifact v98, build 13:21.

**Bug fixed 23/09/2026 (user: "the category waterfalls are missing"):** both budget pages rendered the category section with the SAME canvas ids, so the stretched page drew its charts on the hidden base page's canvases. Canvas ids and the market select now carry a page suffix (`-b` / `-s`, `MID`), the select is found by class. Verified: base group/source/country charts end at 180/180/35, stretch ones at 200/200/36.7, and they survive switching pages. Artifact v99, build 23/09/2026 14:47. Rule: any chart drawn inside renderBudget must have a mode-specific canvas id.

**Base budget open, stretch behind the password (user, 24/09/2026):** `build_artifact.py` now embeds the base budget in PLAIN text as `window.__DASH_BUDGET__` (budget_actions, budget_2027 without rows, budget_2027_board, budget_2027_categories with the stretch scenario stripped by `strip_stretch()`), and encrypts only `{budget_2027_stretch, full budget_2027_categories}` into `window.__DASH_BUDGET_ENC__`. app.js: `buildShell` merges `__DASH_BUDGET__` into DASH before gating; the sidebar group is "Budget" with a non-nested "Budget 2027" entry (id `budget`, the €180.0m page, kicker "Budget 2027", cards still say "2027 base budget") visible to every viewer, and a nested "Stretched budget" (id `budget_stretch`) that appears only after the Admin unlock ("Admin: unlock the stretched budget"; the unlock opens `budget_stretch`). make_test_build.py shims only the stretch payload. Chat: `unlocked()` is now `!!DASH.budget_2027_board` (base answers for everyone), fact-sheet line "BUDGET 2027:", glossary (1) updated; `get_stretch` still needs the unlock. Verified locked (Budget 2027 with drivers, charts, €180.0m grand total; no stretch tab; no €200m data in the plain block) and unlocked (Stretched budget nested, €200.0m). Artifact v100, build 24/09/2026 15:44.
